# End-to-end tests

This directory is reserved for tests against a real `glab` executable and disposable GitLab test project. Keep credentials and project setup outside the mocked `tests/unit/` runner, and add a dedicated opt-in runner when these tests are introduced.
